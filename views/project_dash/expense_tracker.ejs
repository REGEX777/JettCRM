<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Expenses | <%= project.name %></title>
  <link rel="stylesheet" href="/css/output.css">
  <link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />
  <script src="https://kit.fontawesome.com/82eb69e53c.js" crossorigin="anonymous"></script>
</head>

<body class="bg-gray-50">
  <div class="max-w-4xl mx-auto py-10 px-6">
    <div class="flex justify-between items-center mb-6">
      <a href="/projects/v/<%= project._id %>" class="text-blue-600 text-sm font-medium flex items-center">
        <i class="fas fa-arrow-left mr-2"></i> Back to Project View
      </a>
    </div>

    <div class="mb-6">
      <h1 class="text-3xl font-bold text-gray-800"><%= project.name %></h1>
      <p class="text-sm text-gray-500 mt-1">Track all project-related expenses</p>
    </div>

    <form action="/projects/v/expenses/<%= project._id %>" method="POST"
      class="bg-white border border-gray-200 rounded-xl p-6 mb-10 shadow-sm">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm text-gray-600 mb-1">Title</label>
          <input type="text" name="title" required
            class="w-full p-2 border border-gray-300 rounded-md text-sm focus:ring-blue-500 focus:border-blue-500" />
        </div>

        <div>
          <label class="block text-sm text-gray-600 mb-1">Amount ($)</label>
          <input type="number" name="amount" required step="0.01"
            class="w-full p-2 border border-gray-300 rounded-md text-sm focus:ring-blue-500 focus:border-blue-500" />
        </div>

        <div>
          <label class="block text-sm text-gray-600 mb-1">Type</label>
          <select name="type"
            class="w-full p-2 border border-gray-300 rounded-md text-sm focus:ring-blue-500 focus:border-blue-500">
            <option value="Travel">Travel</option>
            <option value="Software">Software</option>
            <option value="Equipment">Equipment</option>
            <option value="Misc">Misc</option>
          </select>
        </div>

        <div>
          <label class="block text-sm text-gray-600 mb-1">Description</label>
          <input type="text" name="description"
            class="w-full p-2 border border-gray-300 rounded-md text-sm focus:ring-blue-500 focus:border-blue-500" />
        </div>
      </div>

      <div class="mt-4 text-right">
        <button type="submit"
          class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded text-sm font-medium">
          Submit Expense
        </button>
      </div>
    </form>
 
    <div class="bg-white border border-gray-200 rounded-xl p-6 shadow-sm">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-xl font-semibold text-gray-800">All Expenses</h2>
        <p class="text-sm text-gray-500">Total: $ <%= expenses.reduce((sum, e) => sum + e.amount, 0).toFixed(2) %></p>
      </div>

      <% if (expenses.length === 0) { %>
      <p class="text-gray-500 text-sm">No expenses recorded yet.</p>
      <% } else { %>
      <div class="overflow-x-auto">
        <table class="w-full text-sm text-left text-gray-700">
          <thead class="bg-gray-100 text-gray-600 uppercase text-xs">
            <tr>
              <th scope="col" class="px-4 py-3">Title</th>
              <th scope="col" class="px-4 py-3">Amount</th>
              <th scope="col" class="px-4 py-3">Category</th>
              <th scope="col" class="px-4 py-3">Date</th>
              <th scope="col" class="px-4 py-3">Added By</th>
            </tr>
          </thead>
          <tbody>
            <% expenses.forEach(e => { %>
            <tr class="border-t">
              <td class="px-4 py-3"><%= e.title %></td>
              <td class="px-4 py-3">₹<%= e.amount.toFixed(2) %></td>
              <td class="px-4 py-3"><%= e.category %></td>
              <td class="px-4 py-3"><%= new Date(e.createdAt).toLocaleDateString('en-IN') %></td>
              <td class="px-4 py-3"><%= e.addedBy.name || 'N/A' %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
</body>

</html>
