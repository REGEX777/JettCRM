<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/output.css">
    <script src="https://kit.fontawesome.com/82eb69e53c.js" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />
    <title>Create Estimate | Your Business Name</title>
</head>

<body class="bg-gray-50">
    <!-- Parent -->
    <% if (success.length > 0) { %>
    <div class="w-full bg-green-500 py-3 flex items-center justify-center text-white font-medium">
        <p><%= success[0] %></p>
    </div>
    <% } %>

    <% if (error.length > 0) { %>
    <div class="w-full bg-red-500 py-3 flex items-center justify-center text-white font-medium">
        <p><%= error[0] %></p>
    </div>
    <% } %>
    
    <div class="flex min-h-screen">
        <%- include('../fragments/sidebar'); -%>

        <div class="flex flex-col w-full ml-[24rem]">
            <%- include('../fragments/header'); -%>
            
            <main class="p-8">
                <div class="max-w-7xl mx-auto">
                    <div class="mb-8">
                        <h1 class="text-3xl font-bold text-gray-900">Edit Estimate</h1>
                        <p class="text-gray-600 mt-2">Edit the values below to update your estimate</p>
                    </div>
                    
                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                        <form action="/tools/estimate/edit/<%= estimate._id %>" method="POST" id="estimateForm" class="divide-y divide-gray-200">
                            
                            <div class="p-6">
                                <h2 class="text-lg font-semibold text-gray-900 mb-4">Client Information</h2>
                                
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <div>
                                        <label for="clientName" class="block text-sm font-medium text-gray-700 mb-1">Client Name</label>
                                        <input 
                                            type="text" 
                                            name="clientName" 
                                            id="clientName" 
                                            value="<%= estimate.client 
                                                ? (estimate.client.firstName + ' ' + estimate.client.lastName) 
                                                : (estimate.clientName || '-') %>"
                                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                                            placeholder="Client or company name">
                                    </div>
                                    
                                    <div>
                                        <label for="clientEmail" class="block text-sm font-medium text-gray-700 mb-1">Client Email</label>
                                        <input 
                                            type="text" 
                                            name="clientEmail" 
                                            id="clientEmail" 
                                            value="<%= estimate.client 
                                                ? (estimate.client.email + ' ' + estimate.client.email) 
                                                : (estimate.clientEmail|| '-') %>"
                                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                                            placeholder="Client or company Email">
                                    </div>
                                    
                                    <div class="md:col-span-2">
                                        <label for="clientAddress" class="block text-sm font-medium text-gray-700 mb-1">Client Address</label>
                                        <input 
                                            type="text" 
                                            name="clientAddress" 
                                            id="clientAddress" 
                                            value="<%= estimate.clientAddress %>"
                                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                                            placeholder="Client or company Address">
                                    </div>
                                </div>
                            </div>

                            <div class="p-6">
                                <h2 class="text-lg font-semibold text-gray-900 mb-4">Items & Services*</h2>
                                
                                <div id="itemsContainer" class="space-y-4">
                                    <% estimate.items.forEach((item, index) => { %>
                                        <div class="grid grid-cols-12 gap-4 items-center">
                                            <div class="col-span-6">
                                                <input 
                                                    name="items[<%= index %>][name]" 
                                                    type="text" 
                                                    placeholder="Item or service name" 
                                                    value="<%= item.name %>" 
                                                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                                                    required>
                                            </div>
                                            <div class="col-span-4">
                                                <input 
                                                    name="items[<%= index %>][price]" 
                                                    type="number" 
                                                    step="0.01" 
                                                    placeholder="Price" 
                                                    value="<%= item.price %>" 
                                                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 price-input" 
                                                    required>
                                            </div>
                                            <div class="col-span-2 flex justify-end">
                                                <button type="button" class="text-gray-400 hover:text-red-500 remove-btn <%= index === 0 ? 'hidden' : '' %>">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </div>
                                        </div>
                                    <% }) %>
                                </div>
                                
                                <button type="button" id="addItem" class="mt-4 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-blue-700 bg-blue-100 hover:bg-blue-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                                    <i class="fas fa-plus mr-2"></i> Add Another Item
                                </button>
                            </div>
                            
                            <!-- tax and shit -->
                            <div class="p-6">
                                <h2 class="text-lg font-semibold text-gray-900 mb-4">Tax & Discount</h2>
                                
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <div>
                                        <label for="taxInput" class="block text-sm font-medium text-gray-700 mb-1">Tax Rate (%)</label>

                                        <input 
                                            type="number" 
                                            name="tax" 
                                            id="taxInput" 
                                            value="<%= estimate.tax %>"
                                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                                            placeholder="Client or company Address">
                                    </div>
                                    
                                    <div>
                                        <label for="discountInput" class="block text-sm font-medium text-gray-700 mb-1">Discount</label>
                                        <div class="flex gap-2">
                                            <input 
                                                type="number" 
                                                name="discount" 
                                                id="discountInput" 
                                                value="<%= estimate.discount !== undefined ? estimate.discount : '' %>" 
                                                class="flex-1 px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                                                placeholder="Amount">

                                            <select 
                                                id="discountType" 
                                                name="discountType" 
                                                class="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                                                <option value="flat" <%= estimate.discountType === 'flat' ? 'selected' : '' %>>$ Flat</option>
                                                <option value="percent" <%= estimate.discountType === 'percent' ? 'selected' : '' %>>% Percentage</option>
                                            </select>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- notes -->
                            <div class="p-6">
                                <label for="notes" class="block text-sm font-medium text-gray-700 mb-2">Notes</label>
                                <textarea name="notes" id="notes" rows="3" 
                                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                                    placeholder="Enter any terms, conditions, or notes for the client..."><%= estimate.notes %></textarea>
                            </div>
                            
                            <!-- total summary -->
                            <div class="p-6 bg-gray-50">
                                <h2 class="text-lg font-semibold text-gray-900 mb-4">Estimate Summary</h2>
                                
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <div class="space-y-3">
                                        <div class="flex justify-between">
                                            <span class="text-gray-600">Subtotal:</span>
                                            <span class="font-medium">$ <span id="subtotalDisplay">0.00</span></span>
                                        </div>
                                        <div class="flex justify-between">
                                            <span class="text-gray-600">Tax:</span>
                                            <span class="font-medium">$ <span id="taxDisplay">0.00</span></span>
                                        </div>
                                        <div class="flex justify-between">
                                            <span class="text-gray-600">Discount:</span>
                                            <span class="font-medium">$ <span id="discountDisplay">0.00</span></span>
                                        </div>
                                        <div class="border-t border-gray-200 pt-3 mt-3 flex justify-between">
                                            <span class="text-gray-900 font-semibold">Total:</span>
                                            <span class="text-xl font-bold text-blue-600">$ <span id="totalDisplay">0.00</span></span>
                                        </div>
                                    </div>
                                    
                                    <div class="flex items-end justify-end">
                                        <button type="submit" class="w-full md:w-auto px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                                            Update Estimate
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </main>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
    <script src="/javascript/editEstimate.js"></script>
</body>
</html>
